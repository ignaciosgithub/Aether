pub func main() -> i32 {
    let a: i32 = 3;
    let b: i32 = 5;
    let result: i32 = if (a < b) {
        if (b > a) { 1 } else { 2 }
    } else {
        if (a == b) { 3 } else { 4 }
    };
    println(result);
    return result;
}
