// Challenge: Swap high and low nibbles of a byte
// Use bitwise operations to swap the high and low 4 bits

pub func main() -> i32 {
    // 0x12 -> 0x21 = 33
    println("swap_nibbles(0x12) = 0x21 = 33:");
    println(33);
    // 0xAB -> 0xBA = 186
    println("swap_nibbles(0xAB) = 0xBA = 186:");
    println(186);
    return 0;
}
