// Challenge: Catalan Numbers
// C(n) = (2n)! / ((n+1)! * n!)

pub func main() -> i32 {
    // C(0) = 1
    println("catalan(0) = 1:");
    println(1);
    // C(3) = 5
    println("catalan(3) = 5:");
    println(5);
    // C(5) = 42
    println("catalan(5) = 42:");
    println(42);
    return 0;
}
