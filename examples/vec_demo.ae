pub func main() -> i32 {
    let v: vec[i64] = vec_new(2);
    println(vec_len(v));
    vec_push(v, 10);
    vec_push(v, 20);
    println(vec_len(v));
    println(vec_pop(v));
    println(vec_len(v));
    println(vec_pop(v));
    println(vec_len(v));
    println(vec_free(v));
    println(vec_free(v));
    return 0;
}
