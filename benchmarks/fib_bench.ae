// Benchmark: Fibonacci
// Computes fib(n) using recursion

func fib(n: i64) -> i64 {
    return if (n < 2) { n } else { fib(n - 1) + fib(n - 2) };
}

pub func main() -> i32 {
    println(fib(40));
    return 0;
}
