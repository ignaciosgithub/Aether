pub func fact(n: i64) -> i64 {
    return if (n <= 1) { 1 } else { n * fact(n - 1) };
}

pub func main() -> i32 {
    println("factorial demo");
    let f: i64 = fact(5);
    println(f);
    return 0;
}
